import DefaultLayout from "../components/DefaultLayout";
import PageHeader from "../components/PageHeader/PageHeader";

const Donors = (props) => {
    return (
        <DefaultLayout>
            <div>
                <PageHeader title="Our Donors" image="background/donor.jpg" />
                <div>
                </div>
            </div>
        </DefaultLayout>
    );
};

export default Donors;
